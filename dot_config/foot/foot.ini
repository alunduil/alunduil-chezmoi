# foot, installed from apt.

[main]
# A replacement font must also cover U+E0B0, which zellij-rate-limit-status
# draws, and lacking it produces no error -- see docs/how-to/foot-terminal.md.
font=JetBrains Mono:size=11

# Sommelier reports a scaling factor of 1 and scales the surface itself. The
# default, auto, sizes fonts by DPI whenever every monitor reports 1, which
# on this 153 DPI panel renders them around 1.6x the requested point size.
dpi-aware=no

[scrollback]
# The backstop for panes zellij is not managing; it keeps its own per pane.
lines=10000

# Tempus Night, by Protesilaos Stavrou. Inlined because the Debian package
# ships no themes directory, and chosen by measuring all 84 themes foot
# publishes against contrast, neutrality and slot distinctness --
# docs/how-to/foot-terminal.md carries those criteria, which any replacement
# has to meet.
[colors]
foreground=e0e0e0
background=1a1a1a

regular0=1a1a1a
regular1=ff929f
regular2=5fc940
regular3=c5b300
regular4=5fb8ff
regular5=ef91df
regular6=1dc5c3
regular7=c4bdaf

bright0=242536
bright1=f69d6a
bright2=88c400
bright3=d7ae00
bright4=8cb4f0
bright5=de99f0
bright6=00ca9a
bright7=e0e0e0
