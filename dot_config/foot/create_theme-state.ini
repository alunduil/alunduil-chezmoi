# Written by foot-theme-auto. Edits are overwritten at the next daylight
# transition; change the schedule, not this file.
#
# Deployed once and then left alone, so the timer's writes are not drift. It
# has to exist: foot.ini includes it, and an absent include is a config error
# rather than a skipped line.
[main]
initial-color-theme=1
