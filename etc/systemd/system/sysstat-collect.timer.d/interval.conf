# sadc samples every minute rather than the packaged ten, so an incident that
# starves the user slice inside a minute still leaves a sample either side of
# it. Clearing OnCalendar first replaces the packaged schedule; assigning it
# without the empty line would add a second one.
[Timer]
OnCalendar=
OnCalendar=minutely
